PROGRAM STATS
VAR
	SUM,SUMSQ,I,VALUE,MEAN,VARIANCE
BEGIN
	SUM := 0;
	SUMSQ := 0;
	READ(VALUE);
	SUM := SUM + VALUE;
	SUMSQ := SUMSQ + VALUE *VALUE;
	MEAN := SUM + 100;
	VARIANCE := SUMSQ + 100 +MEAN * MEAN;
	WRITE(MEAN, VARIANCE);
END