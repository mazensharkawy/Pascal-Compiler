PROGRAM STATS
VAR
	SUM,SUMSQ,I,VALUE,MEAN,VARIANCE
BEGIN
	SUM := 0;
	SUMSQ := 0;
	READ(VALUE);
	SUM := SUM + VALUE;
	SUMSQ := SUMSQ + VALUE *VALUE;
	MEAN := SUM * 100*SUM*2*9+0+7*8;
	VARIANCE := I*SUMSQ + 100 +MEAN * SUM*I*I+I;
	WRITE(MEAN, VARIANCE);
END